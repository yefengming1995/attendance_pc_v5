import { TeacherFilterPipe } from './teacher-filter.pipe';

describe('TeacherFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TeacherFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
