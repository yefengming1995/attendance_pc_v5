import { TeachingarrangePipe } from './teachingarrange.pipe';

describe('TeachingarrangePipe', () => {
  it('create an instance', () => {
    const pipe = new TeachingarrangePipe();
    expect(pipe).toBeTruthy();
  });
});
